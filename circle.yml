machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t d9lounge .

test:
  override:
    - docker run -it d9lounge boot run-test

deployment:
  prod:
    branch: master
    commands:
      - heroku docker:release
