machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker-compose build .

test:
  override:
    - docker-compose up shell boot run-test

deployment:
  prod:
    branch: master
    commands:
      - heroku docker:release
