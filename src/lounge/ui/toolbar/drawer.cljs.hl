(ns lounge.ui.toolbar.drawer
  (:require [lounge.state :as state]
            [lounge.ui.drawer.core :as drawer]))

(def drawer-toolbar
  (drawer/drawer-toggle :toggle (cell= (:drawer-toolbar state/toolbar))))
